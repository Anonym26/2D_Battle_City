![image](https://github.com/user-attachments/assets/3fa6bd3a-cb27-4be2-a52f-d0bce67d6b34) 
# Интелекутальный агент для игры в Battle City на Python.

![Лицензия](https://img.shields.io/badge/license-MIT-blue)

---

## 📝 Описание

**2D Battle City** — это проект, вдохновленный классической аркадной игрой Battle City. 
Основная цель проекта — создание интеллектуального агента, способного играть в игру, 
используя алгоритмы обучения с подкреплением (PPO RL). Агент обучается преодолевать игровые уровни, 
сражаться с врагами и защищать базу. 

Проект создан для экспериментов с RL-алгоритмами, такими как Proximal Policy Optimization (PPO), 
с возможностью дальнейшего улучшения стратегии агента.


Код игры Battle City был адаптирован из репозитория Raitis 
https://github.com/raitisg/battle-city-tanks. 

#### Проект включает:

- Игровую среду, адаптированную для взаимодействия с библиотекой Stable Baselines3 и API Gymnasium.
- Обучающую архитектуру, использующую алгоритм Proximal Policy Optimization (PPO).
- Настраиваемую систему уровней и гибкие настройки для тренировки агентов.

---

## 🔧 Основные функции проекта:
#### 1. Обучение агента с подкреплением:

- Адаптированная игровая среда.
- Использование алгоритма PPO.
- Поддержка логирования через TensorBoard.

#### 2. Гибкость настройки:

- Возможность добавления новых уровней и конфигураций.
- Периодическое сохранение модели для возобновления обучения.
  
#### 3. Тестирование:

- Запуск обученной модели в игровой среде.
- Наблюдение за действиями агента.

---

## 🛠 Требования

- Python 3.8 и выше
- Установленное окружение (см. `requirements.txt`)

---

## 🔄 Установка

1. **Клонируйте репозиторий**:
    ```bash
    git clone https://github.com/Anonym26/2D_Battle_City.git
    cd "ваш-репозиторий"
    ```

2. **Установите зависимости**:
    ```bash
    pip install -r requirements.txt
    ```
    ❗ Дополнитлеьно установите пользовательский модуль "gym_tanks", командой:
     ```bash
    pip install .
     ```
---

## 📚 Используемые библиотеки
Проект реализован с использованием следующих библиотек:

- `gymnasium` — интерфейс для создания среды обучения.
- `stable-baselines3` — RL-фреймворк для обучения агента.
- `numpy` — работа с массивами и числовыми вычислениями.
- `pygame` — реализация игровой механики и визуализации.
- `torch` — реализация нейронных сетей и обработка модели.
- `tensorboard` — логирование результатов обучения и мониторинг процесса.

---

## 📂 Структура проекта

| Модуль (директория)    | Описание                                                                         |
|------------------------|----------------------------------------------------------------------------------|
| `gym_tanks/`           | Содержит игровую среду. Основной модуль `tanks.py`                               |
| `tanks.py`             | Основной код игры, включает управление врагами, игроком, пулями и сменой уровней.|
| `models/`              | Содержит сохранённые модели обученных агентов в формате `.zip`.                  |
| `logs/`                | Содержит логи обучения для визуализации через TensorBoard.                       |
| `levels/`              | Хранятся текстовые файлы, определяющие структуры уровней игры.                   |
| `agent_train.py`       | Основной скрипт для запуска обучения модели.                                     |    
| `agent_load.py`        | Скрипт для тестирования обученной модели.                                        |
| `setup.py`             | Скрипт для установки пользовательского модуля `gym_tanks`.                       |
| `.gitignore`           | Исключает определенные файлы и папки из контроля версий Git.                     |
| `requirements.txt`     | Список всех зависимостей, необходимых для работы проекта.                        |
| `README.md`            | Документация проекта.                                                            |

---

## 📖 Обучение агента: процесс и логика

В этом проекте реализовано обучение интеллектуального агента с использованием алгоритма Proximal Policy Optimization (PPO). 
Этот метод относится к классу on-policy алгоритмов обучения с подкреплением и позволяет эффективно обучать агента 
за счет оптимизации стратегии на основе взаимодействия с окружением.

#### Шаг 1. Создание среды

* Пользовательский модуль `gym_tanks` реализует адаптированную среду для игры 2D Battle City, которая соответствует стандартам Gymnasium API.
* Среда предоставляет данные о текущем состоянии игры, включая:
    + Позиции игрока, врагов, пуль и бонусов.
    + Состояние базы.
    + Текущий этап игры.
* Агент взаимодействует со средой, выполняя действия (движение, стрельба) и получая награды за успешные действия.

#### Шаг 2. Определение вознаграждений
* Агент получает положительные награды за:
    + Уничтожение врагов.
    + Защиту базы.
* Отрицательные награды (штрафы):
    + За попадание под снаряд.
    + За уничтожение своей базы.

#### Шаг 3. Обновление стратегии
* Алгоритм PPO минимизирует разрыв между старой и новой стратегией агента, чтобы избежать больших отклонений и обеспечить стабильное обучение.
* После каждой итерации (например, каждые 2048 шагов) обновляются веса модели, чтобы агент постепенно улучшал свои действия.

---

## 🤖 Обучение модели: agent_train.py

##### 1. Подготовка окружения.
   * Убедитесь, что все зависимости установлены.
   * Запустите виртуальное окружение, если оно настроено.
  
##### 2. Параметры обучения: 
   * Проверьте и при необходимости измените настройки в `agent_train.py`:
       + `TIMESTEPS`: Количество шагов в одной итерации (по умолчанию 2048).
       + `SAVE_INTERVAL`: Интервал сохранения модели (в шагах).
       + `TOTAL_TRAINING_TIMESTEPS`: Общее количество шагов обучения.
       + `test_name`: Имя модели (папка для сохранения результатов).

##### 3. Запуск обучения: введите команду:
```bash
    python agent_train.py
```
Скрипт будет логировать метрики в директорию `logs/` (для TensorBoard), и
сохранять модель в директорию `models/`.

##### 4. Логирование процесса:
Вы можете отслеживать процесс обучения через TensorBoard:
```bash
    tensorboard --logdir=logs
```
Перейдите по ссылке, которая будет выведена в консоли, 
чтобы увидеть метрики (например, `reward`, `timesteps`, `loss`).

##### 5. Периодическое сохранение модели:
Модели сохраняются каждые `SAVE_INTERVAL` шагов в формате `.zip`:
```bash
    models/
└── PPO/
    └── Test_Bot/
        ├── model_1024_steps.zip
        ├── model_2048_steps.zip
        ├── model_4096_steps.zip
```
Последняя модель будет загружаться при следующем запуске, если её путь указан в `weights_path`.
Таким образом сохраняется приемственность и последовательность обучения.

---

## ⌨ Тестирование модели: agent_load.py

#### 1. Подготовка:
* Убедитесь, что обученная модель сохранена в директории `models/`.
Также вы можете протестировать мою обученную модель скачав ее по ссылке: https://disk.yandex.ru/d/xJBukYWSmMqU5g
и разместив архив в директории `models/`.
* Проверьте в `agent_load.py`, что путь к модели соответствует сохранённой модели:
```python
    model_path = "models/PPO/Test_Bot/model_2066432_steps.zip"
```

#### 2. Настройка уровня:
* Если хотите начать тестирование с определённого уровня, проверьте реализацию в `gym_tanks/envs/tanks.py`. 
Уровни переключаются автоматически при вызове метода `nextLevel()`.
* Добавлять новые уровни или усложнять карты в gym_tanks/envs/tanks.py.
* Создавать пользовательские карты в текстовом редакторе при помощи назначенных символов.

#### 3. Запуск тестирования: выполните команду:
```bash
   python agent_load.py
```
Модель будет тестироваться на разных уровнях, управляя агентом. 
Результаты её действий можно наблюдать в игровом окне.

#### 4. Изучение результатов:
* Логирование основых метрик происходит через TensorBoard.
* Также некоторые метрики (например, количество очков) будут отображаться в консоли.
* Если хотите оценить эффективность модели более детально, модифицируйте `agent_load.py`, 
чтобы выводить нужные данные (например, среднее время прохождения уровня).

---

## 📜 Лицензия

Проект распространяется под лицензией [MIT](LICENSE).

---

## 📜 Автор

- 🔗 GitHub: [Anonym26](https://github.com/Anonym26)
- 🚀 Проект создан для изучения алгоритмов обучения с подкреплением
  и реализации интеллектуальных агентов для управления в сложных игровых средах.

---

✨ **Проект 2D Battle City предоставляет мощную базу для экспериментов с обучением агентов на основе RL. 
Создавайте, обучайте и улучшайте свои модели, чтобы добиться максимальной эффективности!** 🚀
